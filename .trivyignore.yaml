# Accepted Trivy findings.
#
# Each entry must have a matching section in docs/SECURITY_EXCEPTIONS.md and an
# expired_at date. Once that date passes Trivy stops honouring the entry, so the
# finding comes back and forces a re-review instead of lingering silently.
vulnerabilities:
  - id: CVE-2026-14257
    statement: >-
      brace-expansion DoS. The only patch (5.0.8) is API-incompatible with the
      minimatch@3 consumers that pull in 1.1.16, and those live exclusively in
      ESLint's dependency tree, which never processes untrusted input. All
      runtime paths already resolve 5.0.8. See docs/SECURITY_EXCEPTIONS.md.
    expired_at: 2026-10-01
